using Moq;
using NUnit.Framework;
using System;
using System.Collections.Generic;
using System.Text;
using System.Threading.Tasks;
using ultra_comment_viewer.src.viemodel;
using ultra_comment_viewer.src.viewLogic;

namespace ultra_comment_viewer_test
{
    class ConnectSwitcherTest
    {/*
        private CommentGenerator generator = new CommentGenerator();
        [Test]
        public async Task SuccessSWitch()
        {
            var liveState = new MainWindowViewModel();
            var switcher = new ConnectSwitcher(liveState, generator);
            await switcher.DoStart("");
            Assert.AreEqual(true, liveState.isConnect);
        }

        [Test]
        public async Task SwicthToFalseStateFromTrue()
        {
            var state = new MainWindowViewModel();
            state.isConnect = true;
            var sw = new ConnectSwitcher(state, generator);
            await sw.DoStart("");
            Assert.AreEqual(false, state.isConnect);
        }*/

      

        
    }
}

<Window x:Class="ultra_comment_viewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:converter="clr-namespace:ultra_comment_viewer.src.view.converter"
        xmlns:local="clr-namespace:ultra_comment_viewer"
        xmlns:prism="http://prismlibrary.com/"
        
        prism:ViewModelLocator.AutoWireViewModel="True"
        mc:Ignorable="d"
    Title="MainWindow" Height="1200" Width="800">
    <Window.Resources>
        <converter:BoolToStringConverter x:Key="TestConverter"/>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="4*" MinHeight="20"/>
            <RowDefinition Height="20*" MinHeight="20"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="180*" MinHeight="40" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="40*" MinWidth="70"/>
            <ColumnDefinition Width="8"/>
            <ColumnDefinition Width="600*" MinWidth="200"/>
        </Grid.ColumnDefinitions>

        <!--================== MenuBar =================================-->
        <md:Card
            Content="Card"
            Grid.ColumnSpan="3"
            Grid.Row="0"
            Background="Chocolate"
            />

        <!--================== DropZoneArea   ==========================-->
        <md:Card
            Grid.Column="0"
            Grid.Row="3"
            Background="Azure"
            >
            <Grid
                Margin="10"
                >
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*"/>
                    <RowDefinition Height="1*"/>
                    <RowDefinition Height="1*"/>
                    <RowDefinition Height="1*"/>
                </Grid.RowDefinitions>
                <!-- URLドロップゾーン -->
                <md:Card
                    Content="URL#"
                    Grid.Row="0"
                    Background="Red"
                    Margin="5,5,5,5"
                    AllowDrop="True"
                    Drop="EventDrop_OnUrlMark"
                    />
                <!-- URLドロップゾーン -->
                <md:Card
                    Content="ユーザー情報"
                    Grid.Row="1"
                    Background="Blue"
                    Margin="5,5,5,5"
                    AllowDrop="True"
                    Drop="Drop_OpenUserInfoWindow"
                    />
                <!-- URLドロップゾーン -->
                <md:Card
                    Content="ログコメント#"
                    Grid.Row="2"
                    AllowDrop="True"
                    Drop="EventDrop_OpenLogWindow"
                    Margin="5,5,5,5"
                    />
                <!-- URLドロップゾーン -->
                <md:Card
                    Content="ユーザーNG"
                    Grid.Row="3"
                    Background="Green"
                    Margin="5,5,5,5"
                    />
            </Grid>
        </md:Card>

        <!--================== URLフォームエリア ========================= -->
        <md:Card
            Grid.Column="2"
            Grid.Row="1"
            Background="AntiqueWhite"
            >
            <StackPanel
                Orientation="Vertical"
                >
                <!-- TwicasURlForm -->
                <StackPanel
                    Margin="20 3 0 0"
                    Orientation="Horizontal"
                    >
                    <TextBox
                        
                        Width="500"
                        Text="{Binding LiveUrlWrittenByTwicasForm, UpdateSourceTrigger=PropertyChanged}"                        TextChanged="TextChanged_ValidateTwicasLiveUrl"
                      
                        md:HintAssist.Hint="Twicasの放送URlを入力して下さい"
                        Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                        VerticalAlignment="Center"
                        
                         />
                    <Button
                        Content="接続"
                        Width="60"
                        Margin="10 10 0 0"
                        Height="25" 
                        Cursor="Hand"
                        Background="Crimson"
                        BorderBrush="Red"
                        IsEnabled="{Binding IsWriteUrl}"
                        Click="EventClick_ConnectionTwicasCommentServer"
                        />
                    <Button
                        Background="DeepSkyBlue"
                        BorderBrush="DodgerBlue"
                        Content="切断"
                        Width="60"
                        Margin="10 10 0 0"
                        Height="25" 
                        Cursor="Hand"
                        IsEnabled="{Binding IsWriteUrl}"
                        Click="EventClick_DisconnectionTwicasServer"
                        />
                </StackPanel>
                
                <!-- NiconicoUrlForm -->
                <StackPanel
                    Margin="20 3 0 0"
                    Orientation="Horizontal"
                    >
                    <TextBox
                        Width="500"
                        Text="{Binding LiveUrlWrittenByNicoNicoForm, UpdateSourceTrigger=PropertyChanged}"                        
                        TextChanged="TextChanged_ValidateNicoNicoLiveUrl"
                        md:HintAssist.Hint="ニコニコの放送URlを入力して下さい"
                        Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                        VerticalAlignment="Center"
                        
                         />
                    <Button
                        Content="接続"
                        Width="60"
                        Margin="10 10 0 0"
                        Height="25" 
                        Cursor="Hand"
                        Background="Crimson"
                        BorderBrush="Red"
                        IsEnabled="{Binding IsWriteNicoNicoUrl}"
                        Click="Click_ConnectionNicoNicoServer"
                        />
                    <Button
                        Background="DeepSkyBlue"
                        BorderBrush="DodgerBlue"
                        Content="切断"
                        Width="60"
                        Margin="10 10 0 0"
                        Height="25" 
                        Cursor="Hand"

                        Click="Click_DisConnectNicoNicoServer"
                        />
                </StackPanel>
                <StackPanel></StackPanel>
            </StackPanel>
        </md:Card>
        <!-- ============ドロップゾーン -*- コメントエリア =================== -->
        <GridSplitter
            Grid.Column="1"
            Grid.Row="3"
            Style="{StaticResource VerticalGridSplitter}"
            />
        <!-- ================= URLフォーム -*- コメントエリア==============  -->
        <GridSplitter
            Grid.Column="2"
            Grid.Row="2"
            Style="{StaticResource HorizontalGridSplitter}"
            />
        <!-- ================= コメントエリア ========================== -->
        <md:Card
            Background="AliceBlue"         
            Grid.Column="2" 
            Grid.Row="3" >
            <ListView 
                x:Name="CommentList"
                ScrollViewer.ScrollChanged="EventScrollChanged_UpdateAutoScrollFlag"
                VirtualizingPanel.IsVirtualizing="True"
                VirtualizingPanel.VirtualizationMode="Recycling"
                >
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel
                            Margin="20 0 20 0"
                            Orientation="Horizontal"
                            Height="50"
                            >
                            <StackPanel
                                Width="70"
                                Margin="0 0 20 0"
                                >
                                <!-- ユーザーアイコン -->
                                <Image 
                                    Height="30"
                                    Width="30"
                                    Source="{Binding Image}"
                                    Grid.Row="0"
                                    Grid.Column="0"
                                   />
                                <!-- ユーザーネーム -->
                                <TextBlock                                    
                                    TextWrapping="Wrap"
                                    Text="{Binding UserName}"
                                    FontSize="8"
                                    VerticalAlignment="Center"
                                    />
                            </StackPanel>

                            <!-- コメント -->
                            <md:Card
                                Cursor="Hand"
                                MouseMove="EventMouseMove_CommentDrag"
                                VerticalAlignment="Top"
                                Width="600"
                                md:UniformCornerRadius="7"
                                Background="{Binding CommentColor}"
                                >
                                <TextBlock
                                    Margin="15"
                                    HorizontalAlignment="Left"
                                    FontSize="10"
                                    Text="{Binding Comment}"
                                />
                            </md:Card>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </md:Card>


    </Grid>
</Window>
